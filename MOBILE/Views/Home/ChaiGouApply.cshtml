<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>采购申请单</title>
    <meta name="viewport" content="width=device-width,initial-scale=1,minimum-scale=1,maximum-scale=1,user-scalable=no" />
    <link rel="shortcut icon" href="~/images/logo.ico" />
    <link rel="bookmark" href="~/images/logo.ico" />

    <link rel="stylesheet" type="text/css" href="~/Styles/bootstrap.min.css" />
    <script src="~/Scripts/jquery-1.10.2.min.js" type="text/javascript" charset="utf-8"></script>
    <link rel="stylesheet" type="text/css" href="~/Styles/login.css" />
    <link rel="stylesheet" type="text/css" href="~/Styles/reset.css" />
    <link rel="stylesheet" type="text/css" href="~/Styles/ChaiGou.css" />
    <script src="~/Scripts/hrefwin.js"></script>
    <style>
        body {
            font-size: 15px;
        }

        .login_main {
            height: 300px;
        }

        .row {
            margin: 10px 0;
            vertical-align: middle;
        }

        .clearfix::after {
            content: "";
            display: block;
            clear: both;
        }

        .btn {
            margin-top: 15px;
        }

        .col-xs-3 {
            height: 34px;
            line-height: 34px;
        }
        .pwd{
            width:100%;
            height:34px;
            line-height:34px;
            border:1px solid #cccccc;
            padding:0 10px;
            display:block;
            border-radius:4px;
        }
        .col-xs-9{
            position:relative;
        }
        .col-xs-9 .glyphicon{
            position:absolute;
            left:25px;
            top:50%;
            transform:translateY(-50%);
            color:#cccccc;
        }
        .col-xs-9 input{
            text-indent:30px;
        }
    </style>
</head>
<body>
    <div class="container">

        <div class="login_main">
            <div class="logo_img"><img src="~/images/logo.png" /></div>
            <form>
            <div class="row clearfix">
                <div class="col-xs-3 text-right">
                    
                    <span>姓名</span>
                </div>

                <div class="col-xs-9">
                    <span class="glyphicon glyphicon-user"></span>
                    <input class="form-control " type="text" name="name" maxlength="7"/>
                </div>

            </div>
            <div class="row clearfix">
                <div class="col-xs-3  text-right"><span>密码</span></div>
                <div class="col-xs-9"><span class="glyphicon glyphicon-lock"></span><input class="form-control pwd"/></div>

            </div>
            <div class="row">
                <div class="col-xs-offset-3 col-xs-9">
                    <a class="btn btn-primary btn-block " id="apply" href="javascript:;">确定</a>
                </div>
            </div>
</form>
        </div>
        <!------dialog3 数量------------->
        <div id="count" class="hide">
            <div><input class="form-control" maxlength="11" /></div>
            <div>
                <button class="btn btn-block">确定</button>
            </div>
        </div>
    </div>
</body>
</html>
<script>
    $("#apply").click(function () {
        var name = $('[name="name"]').val();
        var password = $('.pwd').val();
        $.ajax({
            data: { name: name, password: password },
            type:'post',
            url: "/Home/ApplyLogin",
            success: function (data) {
                if (data.success == '0') {
                    location.href = data.msg
                } else {
                    alert(data.msg+"error");
                }
            },
            error: function (e) {
                console.log(e);
            }
        })
    })
</script>